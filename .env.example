# Garmin Connect credentials
# Copy this file to .env and fill in your details.
# The sync script will prompt for these on first run if not set here,
# then cache a session token in ~/.garminconnect so you only log in once.

GARMIN_EMAIL=your@email.com
GARMIN_PASSWORD=yourpassword
